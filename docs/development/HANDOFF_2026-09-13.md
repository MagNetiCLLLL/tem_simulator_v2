# Physical-tip coherent imaging: round handoff

Date: 2026-09-13. Workspace: `F:\tem_simulator_v2`. Host: `Royal_Gray`.
Local HEAD at wrap-up: `b637e35123ae2463db04e6c2400ac63510b56f95` on `master`.
The checkout contains extensive uncommitted work from multiple rounds. Preserve
it; neither the index nor the remote was changed during this handoff.

## Outcome and acceptance boundary

**The requested new-source TEM and STEM images have not been generated or
qualified. Production source admission remains closed.** The current round is
being handed over, not declared a successful source-to-image implementation.

Actual three-energy joint tip/gun calculations execute. Several numerical
defects have been corrected, and an experimental column route passes C1.
However, the full complex gun output fails an independent numerical-coordinate
convergence check. Passing unit tests, conserving current or selecting Ideal
Optics does not resolve this failure.

The user's latest instruction is to save this handoff and force shutdown.
There is no new commit/push instruction. No Python process was present in the
final process inventory. Previous interrupted diagnostics were already stopped;
their evidence remains on disk. Shutdown issuance and actual power-off are
different facts; the conversation records the command result.

## Required constraints

Read [AGENTS.md](../../AGENTS.md) before continuing. In particular:

- Only the physical emitting tip defines the FEG source. Do not introduce or
  fit an independently configurable accelerated, gun-exit or specimen source.
- Preserve extraction, acceleration, focusing, physical apertures and all
  already modelled column operations. Do not delete Cs or retune the source,
  lens strengths or tolerances to obtain a passing image.
- A downstream equivalent state is a cache of executed upstream transport.
  Bind it to consumed physical, source, model and numerical inputs. A matching
  label or digest is not a replacement for transport.
- Retain every mode's complex field, weight and phase reference. There is no
  single aggregate phase for an incoherent mixture. Optional readout controls
  must not remove physical interactions or detector absorption.
- Dynamic scanning and outgoing inelastic waves must use the same tip-origin
  state. Segment execution and use explicit resource budgets without silently
  dropping physical effects. Preserve old profiles and historical results.
- Respect detector order. Detectors before the filter do not traverse it;
  a requested path reaching the filter must not bypass it. Integrate it last.

## Implemented development work

| Area | Main files under `src/temsim/physics/` | Current scope |
| --- | --- | --- |
| Joint physical source | `surface_gun_wave.py`, `surface_wave.py`, `quadratic_axisymmetric_fem.py` | Curved near-tip FEM joined to a two-way downstream gun load; the interface state is solved, not prescribed |
| Near-tip phase | `radial_phase_fem.py` | Analytic radial carrier with both covariant-gradient terms and consistently transformed source boundary; full complex output restored |
| Grounded gun | `grounded_tip_field.py`, `radial_gun_wave.py` | Axis-regular potential interpolation, analytic aperture/potential moments, closure diagnostics and physical mask scheduling |
| Longitudinal carrier | `liouville_wave.py`, `scattering_load.py` | Exact coordinate transformation, scalar correction and potential-knot derivative jumps; two-way covariant transport |
| Column refinement | `radial_column_wave.py` | Rebuild the executed radial expansion and replay previous operations after refinement; never interpolate a clipped field to invent missing detail |
| GPU transform | `radial_fftlog.py` | Complex128 CPU/CUDA FFTLog with bounded numerical multiplier cache; explicit device/resource errors |
| Admission and routing | `tip_wave_pipeline.py`, `source_admission.py` | Development route available; unresolved side escape rejected; production imaging remains gated |

The scalar stationary relativistic orbital model does not implement spin,
self-consistent space charge or material tunnelling. Unsupported non-axisymmetric
gun fields, Wien/slit optics and offset gun apertures reject explicitly.
The experimental radial column prefix is not a qualified production replacement
for the general Cartesian column operator.

See [the development report](JOINT_SURFACE_GUN_2026-09-13.md) for equations,
assumptions, reference links and the complete evidence narrative.

## Physical reference used in diagnostics

The diagnostic explicitly selects the existing coherent curved-cap reservoir:
100 nm apex radius, 10-degree emitting cap, 100 nA incoming reservoir current,
0.3 eV mean and 0.1 eV RMS emission energy. The grounded-anode voltage convention,
4 kV extraction, electrostatic gun lens, 300 kV acceleration, existing bores,
apertures and shared-column field are retained. This round did not retune the
default TOML files to make a diagnostic pass.

Each completed diagnostic report contains its numerical settings, source/gun
record and mode data. The two latest fine-interface reports also contain the
full instrument snapshot and a separate instrument-settings digest. Treat those
records, not this prose, as the exact reproduction inputs.

The numerical radial coordinate width is a basis choice, not a physical source
width or a new downstream source parameter.

## Validation and execution evidence

Latest focused validation receipt:
[20260913T014348Z-joint-phase-replay-final-127830b7/report.json](evidence/20260913T014348Z-joint-phase-replay-final-127830b7/report.json).

- 106 passed; zero failures, errors or skips; exit code 0.
- Recorded inputs did not change during the run. At handoff, all 656 recorded
  input SHA-256 values still matched current files.
- Serial `compileall` over `src`, `scripts` and `tests` was repeated at handoff
  and passed. This wrap-up changed documentation only; it did not rerun the
  full repository suite or launch another expensive physical calculation.
- Actual CUDA transform regression tests ran. The receipt's generic
  `gpu_validation: NOT_RUN` must not be read as full-chain GPU qualification:
  only the isolated kernel checks and a partial physical prefix were executed.
- No complete new-source image, full-chain convergence or hardware acquisition
  acceptance is claimed.

Important physical execution evidence, relative to `evidence/`:

| Directory | Observed result | Interpretation |
| --- | --- | --- |
| `20260913-fine-interface-coordinate02` | 769 x 257 near grid, 64 radial modes, width ratio 0.2; output 29.2522 nA; interface Gram defect 0.001904 | Complete gun diagnostic, not a qualified source |
| `20260913-fine-interface-coordinate01` | Same physical inputs and numerical counts, width ratio 0.1; output 58.1572 nA | Coordinate convergence fails; relative complex L2 difference is 1.239 without fitted phase/scale |
| `20260913-replay-column-prefix` | First mode propagated beyond C1 to at least 546.5 mm; later requested a larger replay grid | Interrupted; no final prefix checkpoint or image |
| `20260913-cuda-replay-column-prefix` | Same partial-prefix milestone with CUDA acceleration; later requested another refinement | Interrupted; not full-column/GPU image acceptance |

The interrupted directories retain `INTERRUPTED.md` and available arrays/logs.
Earlier coarse-grid failures remain under their original evidence directories.
In particular, the radial phase test at 65 nodes failed its unchanged error
target; 129 nodes passed. Failed data were not deleted or relabelled as success.

## Primary blocker

The global finite Laguerre representation remains strongly dependent on basis
width and mode count after hard gun apertures. Improving the near-field
interface alone has not fixed the complete gun. Finite-basis mask projection
loss can include unresolved wave content and must not all be labelled physical
absorption. Do not renormalize fields, substitute a fitted source or loosen
acceptance tolerances to conceal this error.

The column route also needs a high-dynamic-range representation that handles
hard stops and full aberrations without repeated multi-million-sample replay.
The successful C1 crossing is progress, not proof of a complete column route.

## Resume in this order

1. Converge the full executed complex gun output using independent basis,
   aperture-closure, near-field, electrode-grid, axial-step and domain checks.
   Investigate adaptive/multiscale post-aperture representations before simply
   increasing every dense matrix or replay grid.
2. Complete and verify the axisymmetric prefix, then its state-preserving
   transition into general non-axisymmetric column transport. Keep all phase
   references, interactions and physical masks.
3. Propagate that same executed tip state through the CIF specimen, elastic and
   inelastic channels, timed scan coils, downstream optics and ordered physical
   detectors. Keep TEM and multichannel STEM on the shared source/cache chain.
4. Generate actual images, retain raw per-mode fields and receipts, and perform
   convergence and detector-order checks before enabling production admission.
   A detector blocked by an upstream inserted absorbing detector must not be
   given an invented signal or enabled by silently retracting that detector.

No answer was received to the optional question about accepting an initial
Ideal Optics image versus a full-aberration image. Do not infer permission to
remove aberrations. Either mode still requires a converged physical gun.

## Reproduction entry points

Use the project interpreter from the repository root. The following is an
explicit gun-only diagnostic, not an image command; select an unused output
directory. It can take minutes and may expose further convergence failures.

```powershell
.\.venv\Scripts\python.exe scripts/inspect_surface_column.py --output docs/development/evidence/new-gun-reproduction --stop gun --surface-radial-nodes 769 --surface-axial-nodes 257 --surface-domain 5 --radial-modes 64 --quadrature 128 --gun-pixels 512 --coordinate-width 0.2
```

Use a second unused directory and `--coordinate-width 0.1` for the independent
coordinate check. Compare the directories with `scripts/compare_surface_gun.py`.
Comparisons must match actual physical settings and energy modes; no fitted
phase, curvature or current scale is permitted. Older receipts lacking the
separate settings digest may correctly reject cross-implementation comparison.

`scripts/inspect_gun_matching.py` inspects interface consistency.
`scripts/inspect_radial_fftlog.py` benchmarks isolated transforms only.
`scripts/attempt_default_tip_image.py` uses an older source route and must not
be substituted for new physical-tip acceptance.

To repeat the latest focused suite with a new immutable receipt:

```powershell
.\.venv\Scripts\python.exe scripts/validate_review_p0.py --label joint-phase-replay-handoff tests/test_radial_phase_fem.py tests/test_radial_fftlog.py tests/test_radial_column_wave.py tests/test_radial_analytic_moments.py tests/test_liouville_wave.py tests/test_scattering_load.py tests/test_moving_radial_frame.py tests/test_quadratic_axisymmetric_fem.py tests/test_grounded_tip_surface.py tests/test_surface_wave.py tests/test_surface_wave_integration.py
```

The harness records hashes and offscreen pytest results. Do not edit its inputs
while it runs. Keep all current untracked source, tests, diagnostic evidence and
instrument records; untracked does not mean disposable. No cleanup or remote
upload was part of this wrap-up.
