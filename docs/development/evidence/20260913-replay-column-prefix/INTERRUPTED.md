# Interrupted development execution

The owned CPU diagnostic was stopped after executing the physical tip/gun
and propagating the first mode past C1 to at least Z = 546.5 mm (348.03 s).
Its 900 s budget would not cover all three modes through the round prefix.
The run is being replaced by an explicitly selected, separately validated
CUDA FFTLog implementation, without changing optics or numerical tolerances.

The existing complex-state NPZ is retained. No complete prefix checkpoint,
TEM image, STEM image or convergence acceptance was published by this run.
The executed gun reported current 4.8673435247966114e-8 A and unresolved
side fraction 6.636936161312511e-11; these are diagnostic, unconverged values.
The gun digest printed before prefix execution was
1972ac2a7ae3e97b953af2578b34218458c16405161243e6ae5fa40d357a2b20.

This is an explicit interruption, not a successful completed run.
