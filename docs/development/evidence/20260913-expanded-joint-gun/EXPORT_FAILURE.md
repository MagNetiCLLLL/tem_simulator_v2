# Diagnostic export failure

The calculation produced `complex_states.npz`, but report serialization
failed on immutable nested metadata. No receipt or convergence qualification
was completed. The corrected rerun is stored separately; do not use this
directory as a successful validation result.
