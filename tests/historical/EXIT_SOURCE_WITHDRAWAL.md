# Exit-source withdrawal, 2026-09-11

The user requires physical tip emission followed by all existing gun operations.
An independent high-energy exit distribution is prohibited, even if a digest
binds it to the gun. It is not an upstream computation checkpoint.

The former effective-source activation, particle-dispatch, source-dialog,
profile-activation and request-quality expectations in
`test_effective_gun_source.py` are superseded by `test_tip_source_contract.py`.
That suite checks production rejection, unmodified physical inputs on refusal,
tip-only editing, real gun history/arrival times, aperture blocking, accelerating
and focusing controls, and reuse only after an upstream computation.

Historical Gaussian mode/covariance/energy mathematics and payload decoding
remain tested. `test_gun_wave_transport.py` uses an explicitly local fixture
override for the isolated downstream column and numeric codec. Its particle
entry still rejects the exit source. This override is not installed in the
application and does not provide tip-to-exit or production image evidence.

The original expectations are available in repository history at baseline
`f090376`. Current acceptance remains blocked until the physical coherent gun
and complete specimen/scan/detector chain have been implemented and validated.
